export enum AlbumType{
    album = 'album',
    single = 'single',
    compilation = 'compilation'
}

export enum ModelType{
    artist = 'artist',
    album = 'album',
    track = 'track',
    user = 'user'
}

export enum StandardRestrictionType{
    market = 'market',
    product = 'product',
    explicit = 'explicit'
}
